# Секрети — НІКОЛИ не пакувати в образ (передаються через --secrets-file під час деплою)
.env
токен.txt

# Python-сміття та локальне середовище
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# Тести й службове в продакшн-образі не потрібні
tests/
conftest.py
